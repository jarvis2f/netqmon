# Server-only URL; never expose this value through NEXT_PUBLIC_*.
NETQMON_COLLECTOR_INTERNAL_URL=http://127.0.0.1:8091

# Generate with: openssl rand -base64 32
NETQMON_SESSION_SECRET=replace-with-at-least-32-random-characters
